<p style="color: green"><%= notice %></p>

<%= render @blog %>

<div>
  <%= link_to "Edit this blog", edit_blog_path(@blog) %>
  <%# <%= link_to @blog.status, toggle_status_blog_path(@blog) %> 
  
  <div>
  Status: <%= @blog.status&.capitalize || 'Define a status!' %>
  <% if @blog.draft? %>
    <%= link_to "Toggle to Published", toggle_status_blog_path(@blog) %>
  <% else %>
    <%= link_to "Toggle to Draft", toggle_status_blog_path(@blog) %>
  <% end %>
  </div>


  <%= link_to "Back to blogs", blogs_path %>

  <%= button_to "Destroy this blog", @blog, method: :delete %>
</div>
